Fix these remaining issues in the SolidJS project. All builds must pass after changes. Use bun, never npm.

### 1. notes-page.tsx — Add resource error handling
- Import `formatApiError` from `@/api/client` and `Alert` from `@/components/ui/alert`
- Below `<Suspense>` in the main content, add:
```tsx
<Show when={notes.error}>
  <Alert variant="destructive">{formatApiError(notes.error)}</Alert>
</Show>
```

### 2. events-page.tsx — Add resource error + fix raw button
- Import `formatApiError` and `Alert`
- Add error check below `<Suspense>`
- Replace the raw `<button>` (create event toggle) with `<Button>` component

### 3. exams-page.tsx — Add resource error handling
- Import `formatApiError` and `Alert`
- The exams resource is `list` variable. Add error check: `list.error`

### 4. courses-page.tsx — Add resource error handling
- Import `formatApiError` and `Alert`
- Add error check for `courses` resource

### 5. marks-page.tsx — Add resource error handling
- Import `formatApiError` and `Alert`
- Add error check for `mine` resource

### 6. login-page.tsx
- Remove `tabIndex={-1}` from the password toggle button
- Replace hardcoded `aria-label={"Hide password"}/{"Show password"}` with `t()` using keys `"auth.hidePassword"` and `"auth.showPassword"`
- Add `auth.hidePassword` and `auth.showPassword` to MessageKey type and EN/TR dictionaries in messages.ts

### 7. register-page.tsx
- Add a password confirmation field after the password field
- Add a `confirmPassword` signal
- Validate that passwords match before submit
- Show inline error if they don't match

### 8. admin-users-page.tsx
- When `users()` is empty, show a dashed-border empty state instead of empty table
- Use `t("admin.noUsers")` i18n key — add it to MessageKey type and EN/TR dicts

### 9. live-monitor-page.tsx
- Fix redundant ternary on line ~76: `setError(err instanceof ApiError ? formatApiError(err, locale()) : formatApiError(err, locale()))` → just `setError(formatApiError(err, locale()))`
- Add loading indicator when `snapshot()` is null and `!error()`: show `<PageSpinner />`

### 10. profile-page.tsx
- Remove `void navigate({ to: "/" })` after save — let user stay on profile page

### 11. course-detail-page.tsx
- Add `ConfirmDialog` before calling `deleteCourseEnrollmentByUserId` for roster removal
- Follow the same pattern as `attendance-table.tsx`'s confirm dialog

### 12. exam-detail-page.tsx → use `useParams`
- Replace `decodeURIComponent(location().pathname.split("/")[2] ?? "")` with `useParams(from: "/exams/$id")` 
- Same for `exam-room-page.tsx` and `live-monitor-page.tsx`

### 13. attendance-status-picker.tsx
- Replace `.map()` with `<For>` component for rendering options

### 14. exam-form.tsx
- Add `id` and `aria-label` to the time input fields

### 15. event-detail-page.tsx
- Fix error message when marking attendance with empty user ID: instead of `throw new Error(t("events.userId"))`, use `setError(t("events.userIdRequired"))` and `return`
- Add `events.userIdRequired` to MessageKey and dictionaries

Return a summary of all changes made.
