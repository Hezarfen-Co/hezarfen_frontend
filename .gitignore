# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript
*.tsbuildinfo
.od-skills/
critique.json
playwright-report
test-results

# SurrealDB CLI REPL history (kb queries); see .claude/kb-sync.sh
history.txt

# Browser-verification screenshots land in the repo root; never commit them
/*.png

# Compose stack env files: the operator's knobs (hezarfen_frontend.env) and the
# deploy-owned HEZARFEN_TAG carrier (stack.env) are machine-local, never secrets
# in git. See deploy/hezarfen_frontend.env.example for the tracked template.
hezarfen_frontend.env
stack.env
.env

# The CI deploy bundle (image tarball + compose files + tag) is assembled into
# stage/ by .github/workflows/main.yml; a local rehearsal must not commit it.
stage/

# graphify knowledge graph output
graphify-out/
